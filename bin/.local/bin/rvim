#!/bin/sh

mkdir --parents -- "$RVIM_RUNTIME"

[ -n "$RVIM_TMP" ] && {
	nvim --server "$RVIM_TMP" --remote-wait "$@"
	exit "0"
}

export RVIM_TMP="$RVIM_RUNTIME/$$.sock"
nvim --listen "$RVIM_TMP" "$@"
