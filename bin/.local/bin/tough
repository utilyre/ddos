#!/bin/sh

mkdir --parents -- "$TOUGH_RUNTIME"

export TOUGH_TMP="$TOUGH_RUNTIME/$$.fifo"
mkfifo -- "$TOUGH_TMP"
ueberzug layer --silent < "$TOUGH_TMP" &
exec 3> "$TOUGH_TMP"

lf "$@"
exec 3>&-
rm -- "$TOUGH_TMP"
