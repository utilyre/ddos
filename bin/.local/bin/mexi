#!/bin/sh

action="$(printf "%s\n" "Sleep" "Restart" "Shutdown" | dmenu -p " ")"
[ "$?" -ne "0" ] && exit 1

case "$action" in
	"Sleep") systemctl suspend ;;
	"Restart") systemctl reboot ;;
	"Shutdown") systemctl poweroff ;;
esac
